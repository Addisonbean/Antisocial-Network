<%= form_for(:like, url: like_post_path(post), remote: true) do |f| %>
	<%= f.hidden_field :post_id, value: post.id %>
	<% daClass = post.liked_by?(current_user) ? "liked-post" : "" %>
	<%= f.submit "&#x2764; #{post.likes.count}".html_safe, class: daClass %>
<% end %>


